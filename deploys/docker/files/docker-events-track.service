[Unit]
Description=Docker Resource Usage Tracker (images, volumes, networks)
After=docker.service
PartOf=docker.service

[Service]
Type=simple
ExecStart=%h/.local/bin/docker-events-track.sh
Restart=on-failure
RestartSec=10
KillMode=process

[Install]
WantedBy=default.target
