{
  "name": "AI Agent DevContainer",
  "dockerComposeFile": "./compose.yml",
  "service": "devcontainer",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "customizations": {
    "vscode": {
      "extensions": []
    }
  },
  "remoteUser": "codespace",
  "postStartCommand": "bash -c 'init-ai-devcontainer-home && (git lfs uninstall || true) && echo t3 code starting on port ${OPENCODE_PORT:-4096}... && cd /workspaces/${localWorkspaceFolderBasename} && t3 start --host 0.0.0.0 --port 4096'"
}
